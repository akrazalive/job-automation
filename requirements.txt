# Core web app (dashboard)
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-dotenv>=1.0
python-multipart>=0.0.12   # login/settings form parsing
itsdangerous>=2.2          # signed session cookies (dashboard auth)

# AWS Lambda ASGI adapter (used only when deployed; harmless locally)
mangum>=0.19

# AWS SDK (DynamoDB + S3 storage backend)
boto3>=1.35

# Data models
pydantic>=2.9

# Scrapers (Phase 1+)
playwright>=1.47
beautifulsoup4>=4.12
pyyaml>=6.0

# Resume tailoring (Phase 2) - PDF generation, pure Python, no system deps
reportlab>=4.2
# LLM-assisted bullet-phrasing rewrite - only used when ANTHROPIC_API_KEY
# is set; deterministic skill-reorder tailoring works without it.
anthropic>=0.39
